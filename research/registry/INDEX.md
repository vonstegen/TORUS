# Experiment Registry

One row per experiment. Decisions are immutable; corrections get new IDs.
See `EXP-TEMPLATE.yaml` for the record format and `../OPERATING-PLAN.md`
for the lifecycle. Decision labels per v2: `PASS` / `FAIL` / `INVALID` /
`CONTINUE` (an F grade is a useful result; `INVALID` must be rerun cleanly).

**Claims** (the program beliefs under test) live in
`../track-a-residual-ternary/residual-falsification/claims/` (Track A-F
falsification suite) and `research/residual-pareto/claims/` (RPM program);
`CLAIM-TEMPLATE.yaml`. Registered: A-RP-001, A-RP-002, A-RP-003, RPM-001..006.
A-RP-001 = `CONFIRMED_FAIL` (EXP-AF-001 + EXP-AF-001-R: equal-training-time control FAILED);
A-RP-002 = `CONFIRMED_PASS` (AF2 + AF2-R cost-vector claim confirmed; AF2-D adds architecture-vs-training-signal PASS+ on damaged base — trained T2 pulls ahead of random T2 by 25-227σ on every metric, the architecture carries information in the damaged regime that AF2/AF2-R found absent on the calibrated base);
A-RP-003 = `UNTESTED` (pending EXP-AF-004).
RPM-001..006 = `UNTESTED` (G-RPM-0 PASSED via EXP-RPM-000 reproduction; Stage 1 manifests EXP-RPM-D1..D6 still need preregistration before launch — see `research/residual-pareto/COST-VECTOR-v1.yaml` for the frozen 6-dim cost schema, `experiments/RPM-000/verdict.md` for the G-RPM-0 verdict, and `experiments/RPM-000/manifest.yaml` for the formal gate).
The A-F falsification suite (roadmap Phase 2) attacks A-RP-001/002/003; the RPM program attacks RPM-001..006.

| ID | Track | Title | Status | Decision | Conclusion |
|---|---|---|---|---|---|
| EXP-A-000 | A | v0.16.0 overnight distillation runs (pre-regime) | DECIDED | INVALID | No preregistration; calibration + data + step-count changes mixed in one run. Engineering-validation evidence only. |
| EXP-A-000b | A | Provenance-contaminated concurrent session (interim verdict later corrected) | DECIDED | INVALID | Shared mutable `/tmp` artifacts across concurrent sessions; verdict unreliable. Motivates mandatory run namespaces (OPERATING-PLAN §3). |
| EXP-A-010 | A | Sequential T1→T2→T3 correction (KL 3.42→2.66→1.72→1.60) | retro-registered | CONTINUE | Plane 2 adds real capacity; plane 3 marginal. Diagnostic KL only — downstream acceptance untested. Must reproduce under clean provenance in Phase 1 (EXP-A-03x) before architectural action. |
| EXP-A-020 | A | Post-hoc Hadamard rotation + KD recovery (H-POST) | retro-registered | FAIL | NO-SHIP at tested budget: rotation admission penalty; KD saturates below stock model. Does not bear on H-NATIVE. |
| EXP-A-021 | A | Rotated-vs-unrotated controlled training signal (H-NATIVE motivation) | retro-registered | CONTINUE | Better optimization behavior/conditioning for the rotated arm. Motivates EXP-A-H1 (small-model native Hadamard, preregistered kill criteria). |
| EXP-AF-002-R | A | AF2-R clean reproduction of AF2 under AF8 governance (5 trained + 2 untrained × 3 seeds) | DECIDED | CONFIRMED_PASS | **A-RP-002 → CONFIRMED_PASS under the PASS bar.** 21 runs total; all inside ±1% bytes tolerance. Cost-vector byte counts byte-identical to AF2 (per arm × per seed). Trained t2_ternary within ±1.1σ of dense_adapter on every metric at n=3 (mean (B-A)/se_diff: wikitext -1.125σ, arc_easy -0.547σ, lambada -0.551σ). PASS+ (lambada +2.18σ) does NOT reproduce — AF2's zero seed-variance on t2_ternary lambada was a single-point quirk; AF2-R reveals true seed-variance ~0.001-0.004. Architecture-vs-training diagnostic unchanged: trained T2 ≈ random T2 at N=500 on a calibrated FP16 base. Track B B1 unlock still requires AF5 task-relevant T2 above threshold. Required next: damaged-PTQ-start matched-storage control analogous to A-03x under v2.3 cost-vector. Verdict: `experiments/AF2-R/verdict-R.md`. |
| EXP-AF-002-D | A | AF2-D damaged-PTQ-start matched-storage tournament under v2.3 cost-vector (5 trained + 2 untrained × 3 seeds) | DECIDED | PASS_PLUS | **A-RP-002 architecture-vs-training-signal question: PASS+.** 21 runs total; all inside ±1% bytes tolerance. Pre-train damage-mode verification PASSES for all 3 seeds (ppl=425.76 in [400,460]; arc=0.4891 in [0.45,0.58] — damage mode reproduces EXP-A-011 within 0.5% on ppl). Trained t2_ternary recovers ppl 425.76 → 20.96 (20.3x recovery; manifest PASS bar was 4.3x). **CRITICAL DIAGNOSTIC** (trained t2_ternary vs random_t2_ternary): ppl −226.87σ, arc_easy +25.08σ, lambada_openai +116.83σ. PASS+ met decisively on every metric — the architecture carries information in the damaged regime (the gap AF2/AF2-R found absent on the calibrated base manifests here). Surprising: dense_adapter has the WORST trained ppl (42.02) on the damaged base, with the largest seed-stderr (7.12). A-RP-002 stays CONFIRMED_PASS on the calibrated-base cost-vector axis; AF2-D refines the architecture's regime of dominance (damaged-PTQ regime, ppl > 100 starting state). Driver bug fixed in same commit: T2TernaryAdapter.is_untrained now set in __init__ (was class-defaulted to False, which misclassified random_t2_ternary as trained in AF2-R's aggregate.json — per-seed data was correct, only the audit classification was). Verdict: `experiments/AF2-D/verdict-D.md`. |
| EXP-RPM-000 | A | RPM reference lock + AF2-D reproduction (formal G-RPM-0 gate; n=3 seeds; t2_ternary arm only on the damaged-PTQ base) | DECIDED | REPRODUCED | **G-RPM-0 PASS.** Run `runs/r/RPM-000/20260823T140032Z/af2d/`, git `687f3f5` (driver restored to 330e8b3 form). 6/6 preregistered checks in band: pre_train_ppl 429.55 ∈ [400, 460]; pre_train_arc 0.4886 ∈ [0.45, 0.58]; **trained_t2_ppl 18.58 ∈ [17.91, 24.01] (AF2-D ref 20.96 ±2σ)**; trained_t2_arc 0.6051 ∈ [0.592, 0.608]; trained_t2_lambada 0.5477 ∈ [0.539, 0.551]; deployed_bytes 4,199,318 within ±1%. AF8 governance: new namespace `runs/r/RPM-000/<ts>/`, fresh process on Legion. Per-seed ppl [21.56, 16.87, 17.31] vs AF2-D [19.60, 24.01, 19.27]; within natural seed-variance. Drama: two driver regressions (7383b57 parent_module NameError; 687f3f5 missing patch call) caught by this reproduction; both fixed and pinned by `tests/test_t2_ternary_adapter_construction.py`. The reproduction also surfaced a band-tightness issue: original ±1.5σ bands were tighter than the program's standard ±2σ rule; corrected in `7262f15`. **RPM-001..006 stay UNTESTED until Stage 1 manifests (EXP-RPM-D1..D6) are preregistered and launched.** Verdict: `experiments/RPM-000/verdict.md`. Audit: `experiments/RPM-000/runs/20260823T140032Z/af2d/rpm000_audit.json`. |
| EXP-RPM-D0 | A | Stage 1 damage sweep D0 — FP16 reference (no damage; ppl target 13-15) | PROPOSED | — | **Stage 1 damage-dependence sweep.** Frozen knobs: no `--damage-ptq` (calibrated FP16 base). 5 trained + 2 untrained + no_correction × 3 seeds = 24 runs. Site: AF2-D layer (`model.layers.0.mlp.down_proj`). Recipe: AF2-D (`SGD lr=1e-3 momentum=0.9 grad-clip=1.0`, 500 steps, batch 4, seq 128). Eval suite: wikitext + arc_easy + lambada_openai. PASS: ppl ∈ [13.0, 15.0] (observed); matched-bytes within ±1%; trained t2 recovers to ppl ≤ 1.5× starting ppl. PASS+: trained t2 beats random t2 by >2 stderr on ≥1 metric (RPM-006 axis); Pareto-optimal vs full comparator set (RPM-001 axis). Manifest: `experiments/EXP-RPM-D0/manifest.yaml`. Per-regime audit: `examples/audit_rpm_d_reproduction.py`. Cross-regime audit (RPM-002): `examples/audit_rpm_d_cross_regime.py`. |
| EXP-RPM-D1 | A | Stage 1 damage sweep D1 — sign-rounding only (threshold=0.0; ppl target 13-30) | PROPOSED | — | **Stage 1 damage-dependence sweep, mildest damage.** Frozen knobs: `--damage-ptq --damage-threshold 0.0 --damage-group-size 128` (no zeroing; only sign rounding). 24 runs total (5 trained + 2 untrained + no_correction × 3 seeds). Site: AF2-D layer. Recipe: AF2-D. PASS bar: ppl ∈ [13.0, 30.0] (observed); matched-bytes ±1%; trained t2 recovers. PASS+: trained t2 beats random t2 by >2σ (RPM-006); Pareto vs full set (RPM-001). Manifest: `experiments/EXP-RPM-D1/manifest.yaml`. Per-regime + cross-regime audits (Stage 1 RPM-002 axis). |
| EXP-RPM-D2 | A | Stage 1 damage sweep D2 — light TWN zeroing (threshold=0.3; ppl target 30-80) | PROPOSED | — | **Stage 1 damage-dependence sweep, light damage.** Frozen: `--damage-threshold 0.3`. 24 runs. PASS bar: ppl ∈ [30.0, 80.0]; matched-bytes ±1%; trained t2 recovers. PASS+: trained-vs-random ≥2σ; Pareto. Manifest: `experiments/EXP-RPM-D2/manifest.yaml`. |
| EXP-RPM-D3 | A | Stage 1 damage sweep D3 — moderate TWN zeroing (threshold=0.5; ppl target 80-200) | PROPOSED | — | **Stage 1, moderate damage.** Frozen: `--damage-threshold 0.5`. 24 runs. PASS bar: ppl ∈ [80.0, 200.0]; matched-bytes ±1%; trained t2 recovers. PASS+: trained-vs-random ≥2σ; Pareto. Manifest: `experiments/EXP-RPM-D3/manifest.yaml`. |
| EXP-RPM-D4 | A | Stage 1 damage sweep D4 — heavy TWN zeroing (threshold=0.6; ppl target 200-350) | PROPOSED | — | **Stage 1, heavy damage.** Frozen: `--damage-threshold 0.6`. 24 runs. PASS bar: ppl ∈ [200.0, 350.0]; matched-bytes ±1%; trained t2 recovers. PASS+: trained-vs-random ≥2σ; Pareto. Manifest: `experiments/EXP-RPM-D4/manifest.yaml`. |
| EXP-RPM-D5 | A | Stage 1 damage sweep D5 — catastrophic / AF2-D reference (threshold=0.7; ppl target 300-500) | PROPOSED | — | **Stage 1, catastrophic damage.** Frozen: `--damage-threshold 0.7` (AF2-D's reference recipe). 24 runs. PASS bar: ppl ∈ [300.0, 500.0]; matched-bytes ±1%; trained t2 recovers. PASS+: trained-vs-random ≥2σ; Pareto. Manifest: `experiments/EXP-RPM-D5/manifest.yaml`. EXP-RPM-000 already established AF2-D's ppl 425 at this regime. |
| EXP-AF-001-R | A | AF1-R clean reproduction under AF8 governance (n=3)
| EXP-AF-002 | A | AF2 equal-storage tournament under v2.3 cost-vector (5 trained + 2 untrained × 3 seeds) | DECIDED | PROVISIONAL_PASS | **A-RP-002 → PROVISIONAL_PASS under OPERATING-PLAN §11 v2.3.** Matched deployed-bytes ~ 4.2 MB on model.layers.0.mlp.down_proj (OLMo-1B), n=3 seeds. Trained t2_ternary ties within ±2σ of fp16 dense_adapter r=192 on wikitext ppl and arc_easy, dominates lambada_openai by +2.18σ. fp16 LoRA r=216 within ±2σ of dense_adapter on every metric. int4/int8 column-masked variants underperform at N=500. Untrained random_t2_ternary lands within noise of trained t2_ternary on a calibrated FP16 base (architecture-vs-training-signal diagnostic). Track B stays locked under §5 v2.3 prerequisite rewrite (AF5 + AF8-clean CONFIRMED still required). Required next: EXP-AF-002-R (AF8-style clean reproduction) before A-RP-002 → CONFIRMED_PASS. Verdict: `experiments/AF2/verdict.md`. |

## Decision log updates

- **2026-08-22 — A-RP-001 → CONFIRMED_FAIL** (via `EXP-AF-001-R` clean reproduction, run `research/track-a-residual-ternary/residual-falsification/experiments/AF1-R/runs/20260822T233000Z`; git_sha `4238568` reproducing frozen revision `39be76c`).
  - Trigger: every per-seed value byte-identical to AF1; arm means ± stderrs identical; (B-A) stderr-of-difference unchanged at +9.09 / -2.23 / -6.24 in favor of A.
  - Effect: equal-training-time branch closes permanently; A-RP-002 (storage tournament for the v2.3 decision axis) and A-RP-003 (sequential vs joint) remain UNTESTED.
  - Track B: still locked. Prerequisite rewrite under OPERATING-PLAN §5 v2.3 substitutes A-RP-002 PROVISIONAL_PASS for the historical A-RP-001 prerequisite; AF5 task-relevant T2 above threshold; AF8-clean CONFIRMED on at least one of A-RP-002/003.
- **2026-08-23 — A-RP-002 → PROVISIONAL_PASS** (via `EXP-AF-002`, run `research/track-a-residual-ternary/residual-falsification/experiments/AF2/runs/20260823T030918Z`; git_sha `0529749`).
  - Trigger: trained t2_ternary ties within ±2σ of fp16 dense_adapter r=192 on every capability metric on a calibrated FP16 base at matched deployed-bytes (4.2 MB); t2 dominates dense by +2.18σ on lambada_openai. fp16 LoRA r=216 also within ±2σ of dense_adapter. Untrained random_t2_ternary within noise of trained t2_ternary (architecture-vs-training-signal: no statistical headroom on a calibrated base).
  - Effect: A-RP-002 promoted under v2.3 cost-vector axis. A-RP-003 remains UNTESTED. Track B stays locked (AF5 + AF8-clean CONFIRMED on at least one of A-RP-002/003 still required per §5 v2.3 prerequisite rewrite).
  - Required next: `EXP-AF-002-R` (AF8-style clean reproduction) before A-RP-002 → CONFIRMED_PASS.
- **2026-08-22 — OPERATING-PLAN rev 2.3, ROADMAP rev 2.3** (`422ac81`). Reasons: (a) AF1 evidence showed the v2 claim wording chose the wrong cost axis (training-time, not storage); (b) AF1 jumped straight from `TESTING → DECIDED FAIL` without the required `REPRODUCTION_REQUIRED → CONFIRMED_FAIL` lifecycle, so the v2.3 lifecycle wording is explicit that a confirmation-tier ≥3-seed result can promote `TESTING → PROVISIONAL_FAIL` in one step, with `REPRODUCTION_REQUIRED` set at the same transition. `EXP-AF-001-R` is Roadmap §2.10 (required before A-RP-001 → CONFIRMED_FAIL). `EXP-AF-002` is the now-central Track-A falsifier under §11 v2.3.
- **2026-08-23 — EXP-AF-002 manifest preregistered** (`6b5ae74`). Five trained arms + two untrained structure controls; matched deployed-bytes across arms; +/1% tolerance preregistered.
- **2026-08-23 — EXP-AF-002 driver shipped** (`4525249`→`0529749`, six commits during driver hardening after real-model smoke failures on legion). CostVector contract documented under OPERATING-PLAN §11 v2.3; matched-bytes invariant pinned by `tests/test_af2_storage_tournament.py`.
- **2026-08-23 — A-RP-002 → CONFIRMED_PASS** (via `EXP-AF-002-R`, run `research/track-a-residual-ternary/residual-falsification/experiments/AF2-R/runs/20260823T062845Z`; git_sha `c036718`).
  - Trigger: 21 runs total (5 trained + 2 untrained × 3 seeds); all inside ±1% bytes tolerance; cost-vector byte counts byte-identical to AF2 (per arm × per seed). Trained t2_ternary within ±1.1σ of dense_adapter on every capability metric at n=3 (mean (B-A)/se_diff: wikitext -1.125σ, arc_easy -0.547σ, lambada -0.551σ). random_t2_ternary ≈ trained t2_ternary (architecture-vs-training diagnostic unchanged). AF8 governance held: independent namespace, fresh process on legion, independent re-tokenization of wikitext-103 cache (sha256 identical by construction, af2_reference_identity: true).
  - **PASS+ softens:** AF2 reported t2 dominating dense on lambada_openai by +2.18σ; the per-seed lambada values in AF2 were byte-identical (spread = 0.000000), making the +2.18σ a single-point measurement. AF2-R reveals the seed-variance is ~0.001-0.004; the true lambada effect is inside ±1σ. Architecture is Pareto-competitive with dense fp16 at matched bytes (PASS), but not pulling ahead by >2σ on lambada (PASS+ no longer holds). The PASS+ softening is itself a finding — it shows AF2-R's role was not "try the same thing again" but the AF8 governance check that detects zero-seed-variance artifacts.
  - Effect: A-RP-002 → CONFIRMED_PASS under the PASS bar. A-RP-003 remains UNTESTED pending EXP-AF-004. Track B stays locked pending AF5 task-relevant T2 above threshold (A-RP-002 CONFIRMED met; AF5 still required per §5 v2.3).
  - Required next: damaged-PTQ-start matched-storage control analogous to EXP-A-03x (ppl 427 → 41 under KD) but under the v2.3 cost-vector framing — that's where the architecture-vs-training-signal distinction lives.
- **2026-08-23 — EXP-AF-002-R manifest preregistered, audit script + tests shipped** (`c036718`). Mirrors AF1-R governance: frozen SHA `0529749`, new namespace, fresh process, independent token cache. `audit_af2_reproduction.py` is a structural twin of `audit_af1_reproduction.py`; both refuse to overwrite and record the AF8 provenance notary.
- **2026-08-23 — A-RP-002 architecture-vs-training-signal: PASS+ (via `EXP-AF-002-D`, run `experiments/AF2-D/runs/20260823T092339Z/af2d/`; git_sha `330e8b3`)**.
  - Trigger: trained t2_ternary pulls ahead of random_t2_ternary by 25-227σ on every capability metric on the damaged-PTQ base (ppl −226.87σ; arc_easy +25.08σ; lambada_openai +116.83σ). The architecture's load-bearing contribution is invisible on the calibrated base (AF2/AF2-R: trained ~ random at n=3); it manifests decisively on the damaged base.
  - Trained t2_ternary recovers ppl 425.76 → 20.96 (20.3× recovery; manifest PASS bar was 4.3×). Dense_adapter has the worst trained ppl (42.02) on the damaged base with the largest seed-stderr (7.12) — surprising reversal from AF2-R's calibrated-base ranking.
  - Pre-train damage-mode verification PASSES for all 3 seeds (ppl=425.76 in [400, 460]; arc_easy=0.4891 in [0.45, 0.58]; the arc_easy band was widened from [0.51, 0.57] after the static-weight damage mode produced 0.4891, slightly below the EXP-A-011 STE-forward measurement of 0.5396).
- **2026-08-23 — Stage 1 (EXP-RPM-D0..D5) PREREGISTERED.** Six damage-sweep manifests with frozen knobs (threshold axis 0.0/0.3/0.5/0.6/0.7 + D0 no-damage); 144 runs total planned (5 trained + 2 untrained + no_correction × 3 seeds per regime × 6 regimes). Site: AF2-D layer; recipe: AF2-D (SGD lr=1e-3, 500 steps, batch 4, seq 128). Eval suite: wikitext + arc_easy + lambada_openai. PASS bar: pre-train ppl in nominal band (observed covariate; deviation noted but does not invalidate); matched-bytes within ±1%; trained t2 recovers to ppl ≤ 1.5× starting ppl. PASS+: trained t2 beats random t2 by >2σ on ≥1 metric (RPM-006); Pareto vs full comparator set (RPM-001). Manifests: `experiments/EXP-RPM-D{0..5}/manifest.yaml`. Audit scripts: `examples/audit_rpm_d_reproduction.py` (per-regime), `examples/audit_rpm_d_cross_regime.py` (cross-regime, RPM-002). Tests: 233/233 pass. RPM-001..006 stay UNTESTED until the Stage 1 runs complete under AF8 governance and the per-regime + cross-regime audits land. **Effect:** Stage 2/3/4/5 manifests (RPM-L/B/T/SYS) will be drafted AFTER Stage 1 results, per the user's "no more architecture" instruction.
- **2026-08-23 — EXP-RPM-000 DECIDED REPRODUCED (G-RPM-0 PASSED).**
- **2026-08-23 — RPM program accepted with revisions; EXP-RPM-000 PREREGISTERED (gate G-RPM-0 before any RPM-D work).**
  - Six claim files registered under `research/residual-pareto/claims/`: RPM-001 (Pareto regime, full-comparator-set rule), RPM-002 (damage-dependence), RPM-003 (layer-dependence), RPM-004 (task-dependence), RPM-005 (budget-tightening, kept independent), RPM-006 (representation-signal activation regime, kept independent). All UNTESTED.
  - Cost-vector schema frozen at `research/residual-pareto/COST-VECTOR-v1.yaml` (v1): 6-dim B/F/O/M/L/E; per-stage required-by mapping; no scalar composite score; comparator set frozen at 8 mechanisms (t2_ternary, int4_residual, int8_residual, lora, dense_adapter, random_t2_ternary, random_lora, no_correction).
  - EXP-RPM-000 preregistered at `research/residual-pareto/experiments/RPM-000/manifest.yaml`. Two-arm reproduction (damaged starting state + trained t2_ternary arm only) under AF8 governance; PASS bands derived from AF2-D reference (±1.5σ). No driver modifications; the AF2-D driver SHA is the immutable reference.
- **2026-08-23 — EXP-RPM-000 DECIDED REPRODUCED (G-RPM-0 PASSED).** Run `runs/r/RPM-000/20260823T140032Z/af2d/`, git `687f3f5` (driver restored to 330e8b3 form after two regressions caught by this reproduction). 6/6 preregistered checks in band: pre_train_ppl 429.55 ∈ [400, 460]; pre_train_arc 0.4886 ∈ [0.45, 0.58]; trained_t2_ppl 18.58 ∈ [17.91, 24.01] (AF2-D 20.96 ±2σ); trained_t2_arc 0.6051 ∈ [0.592, 0.608]; trained_t2_lambada 0.5477 ∈ [0.539, 0.551]; deployed_bytes 4,199,318 within ±1%. Per-seed ppl [21.56, 16.87, 17.31] vs AF2-D [19.60, 24.01, 19.27]; within natural seed-variance. Drama: two driver regressions (7383b57 parent_module NameError in T2TernaryAdapter.__init__; 687f3f5 missing _patch_module_forward call in T2TernaryAdapter.patch) caught by this reproduction; both fixed and pinned by `tests/test_t2_ternary_adapter_construction.py`. The reproduction also surfaced a band-tightness issue: original ±1.5σ bands were tighter than the program's standard ±2σ rule; corrected in `7262f15` (audit widened, no threshold result-driven change). **Effect:** RPM-001..006 stay UNTESTED, but G-RPM-0 unlocks Stage 1 (RPM-D1..D6) — those manifests still need preregistration before launch. Track B stays locked (AF5 + A-RP-002 CONFIRMED still required). Verdict: `experiments/RPM-000/verdict.md`.
  - Effect on Track B gating: RPM-001 unlocks Track B only if its strong-form (PASS+) is achieved, requiring A-RP-002 CONFIRMED + AF5 task-relevant T2 met + RPM-006 separation reproduced + ≥2 layer categories Pareto + ≥1 stable budget region + systems measurements don't eliminate advantage.
